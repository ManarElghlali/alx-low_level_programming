#include "main.h"
/**
 * _abs - entry pint that computes the absolute value of an integer
 *
 *@n: int
*
 *Return: Always 0.
 *
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
