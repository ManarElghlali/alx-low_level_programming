#include "main.h"
/**
 * add -  add two value of int type.
 *
 * @x: is an int numbers
 *
 * @z: is int number
 * Return: Integer value.
 */
int add(int x, int z)
{

	int sum = x + z;

	return (sum);
}
